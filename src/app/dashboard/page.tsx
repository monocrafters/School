import UserDashboard from "@/components/dashboard/UserDashboard";

export const metadata = {
  title: "My Application | GPS",
};

export default function DashboardPage() {
  return <UserDashboard />;
}
